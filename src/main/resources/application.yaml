spring:
  application:
    name: synapse

  datasource:
    url: jdbc:postgresql://localhost:5432/synapse
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: test

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql=true

#------------flyway------------
  flyway:
    enabled: true


#-----------Actuator--------------
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always